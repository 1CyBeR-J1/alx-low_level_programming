#include <stdio.h>
#include "main.h"

/**
 * print_to_98 - prints natural number to 98
 * @n: number passed
 *
 * Return: Always 0 (success)
 */

void print_to_98(int n)
{
	for (n <= 98; n <= 98; n++)
	{
		printf("%d, ", n);
	}
	for (n >= 98; n >= 98; n--)
	{
		printf("%d, ", n);
	}
}

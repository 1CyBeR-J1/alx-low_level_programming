#include "main.h"

/**
 * _abs - to get absolute value of an integer
 * @n: interger to be checked
 *
 * Return: The absolute value of n
 */

int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	return (-n);
}
